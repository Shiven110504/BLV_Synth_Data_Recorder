"""Command-line entry points.

``blv-collect list`` runs on vanilla Python (no Isaac boot needed) —
everything else spins up a headless ``SimulationApp`` via
:mod:`blv.synth.data_collector.cli.bootstrap`.
"""
