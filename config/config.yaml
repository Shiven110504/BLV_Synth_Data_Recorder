# BLV Synth Data Collector — User Configuration
# ================================================
# Edit this file to set persistent defaults that carry across IsaacSim runs.
# Values here override the extension.toml defaults but can still be changed
# at runtime through the UI.

# --- Project ---
root_folder: "/home/shiven/Desktop/SDG/Synth_Data"
environment: "hospital_hallway"

# --- Camera ---
camera_path: "/World/BLV_Camera"
move_speed: 60.0
look_speed: 30.0
focal_length: 28.0   # in mm — USD uses "tenths of a scene unit"; see camera setup

# --- Rendering ---
resolution_width: 1280
resolution_height: 720
rt_subframes: 4

# --- Asset Browser ---
# asset_root_folder is the parent directory; the class name below is also
# the subfolder inside it, so assets are scanned at:
#   {asset_root_folder}/{asset_class_name}/*.usd*
asset_root_folder: "/home/shiven/Desktop/SDG/synthetic_objects"
asset_class_name: "elevator_button"
target_prim_path: "/World/TargetAsset"

# --- Data Capture (annotators) ---
# Toggle which annotators the BasicWriter enables.
annotators:
  rgb: true
  semantic_segmentation: true
  colorize_semantic_segmentation: true
  bounding_box_2d_tight: true
  bounding_box_2d_loose: false
  bounding_box_3d: false
  instance_segmentation: false
  normals: false
  distance_to_image_plane: false
