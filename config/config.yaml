# BLV Synth Data Collector — User Configuration
# ================================================
# Edit this file to set persistent defaults that carry across IsaacSim runs.
# Values here override the extension.toml defaults but can still be changed
# at runtime through the UI.

# --- Project ---
# environment + asset_class_name are typically left blank here so the user
# enters them in the UI at the start of a session and clicks "Apply Settings"
# to resolve all derived paths.

root_folder: "/media/shiven/NeurIPS_SSD/Synthetic_Data/collected_data"
asset_root_folder: "/media/shiven/NeurIPS_SSD/Synthetic_Data/synthetic_objects"
environments_folder: "/media/shiven/NeurIPS_SSD/Synthetic_Data/environments"

# root_folder: "/media/shiven/SSD_1TB/Synthetic_Data/collected_data"
# asset_root_folder: "/media/shiven/SSD_1TB/Synthetic_Data/synthetic_objects"
# environments_folder: "/media/shiven/SSD_1TB/Synthetic_Data/environments"
asset_class_name: ""
environment: ""
location: ""

# Multi-class CLI (``blv-collect collect-classes``).  When non-empty,
# ``collect-classes`` regenerates data for each listed class in turn,
# inside a single SimulationApp boot.  When empty, ``collect-classes``
# falls back to ``asset_class_name`` as a one-element list.
# Examples:
#   classes: ["handrail", "stairs", "doorway"]
classes: []

# --- Camera ---
camera_path: "/World/BLV_Camera"
move_speed: 20.0
look_speed: 30.0
focal_length: 28.0   # in mm — USD uses "tenths of a scene unit"; see camera setup

# --- Rendering ---
resolution_width: 1280
resolution_height: 720
rt_subframes: 4
frame_step: 50 

# --- Asset Browser ---
# asset_root_folder is the parent directory; the asset_class_name from the
# Project section above is also the subfolder name, so assets are scanned at:
#   {asset_root_folder}/{asset_class_name}/*.usd*

parent_prim_path: "/World"

# --- Data Capture (annotators) ---
# Toggle which annotators the BasicWriter enables.
annotators:
  rgb: true
  semantic_segmentation: true
  colorize_semantic_segmentation: true
  bounding_box_2d_tight: true
  bounding_box_2d_loose: false
  bounding_box_3d: true
  instance_segmentation: false
  normals: true
  distance_to_image_plane: true
